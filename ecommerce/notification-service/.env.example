# Environment template for notification-service.
# Copy to `.env` and customize for local dev. `.env` is gitignored.
# Spring Boot loads `.env` automatically via spring.config.import in application.yaml.

DB_DRIVER=org.postgresql.Driver
DB_URL=jdbc:postgresql://localhost:5441/notificationdb
DB_USER=notificationuser
DB_PASSWORD=notificationpass
JPA_DIALECT=org.hibernate.dialect.PostgreSQLDialect
JPA_DDL=validate
FLYWAY_ENABLED=true
REDIS_HOST=localhost
REDIS_PORT=6379
KAFKA_BOOTSTRAP=localhost:9092
MAIL_HOST=localhost
MAIL_PORT=1025
MAIL_USERNAME=
MAIL_PASSWORD=
SERVER_PORT=8090
JWT_SECRET=change-me-change-me-change-me-change-me-please-32chars
EUREKA_URL=http://admin:admin123@localhost:8761/eureka/
